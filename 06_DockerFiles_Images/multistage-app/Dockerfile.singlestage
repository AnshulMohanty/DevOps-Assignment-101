# Single-stage build, kept only to compare image size against the multi-stage one.
# Everything - dev dependencies, npm cache, build tooling - stays in the final image.
FROM node:24-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3000
CMD ["npm", "start"]
